/**
 * Temporary rollout switch for the administrative workspace.
 *
 * Keep the admin routes available for direct access while this is `false`.
 * Change it to `true` when the panel is ready to become the primary place for
 * user and category management.
 */
export const ADMIN_PANEL_ENTRY_POINTS_ENABLED = false;
